[project]
name = "stream-test-python"
version = "0.1.0"
description = "High-performance Python Kafka streaming test"
dependencies = [
    "confluent-kafka>=2.3.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
